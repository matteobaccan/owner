# IntelliJ IDEA
.idea/
*.iml

# Maven and Macs
target/
.DS_Store
*.xml.versionsBackup

# Eclipse IDE
.classpath
.settings
.project
.ant-targets-build.xml
.vscode

# Website build (owner-site/web). package-lock.json is deliberately NOT ignored:
# the publish workflow runs `npm ci`, which requires it.
node_modules/
owner-site/web/dist/
owner-site/web/.astro/

# The slide deck rendered into the site by the publish workflow; the source is
# presentation/*.md and the built page is served from gh-pages.
owner-site/web/public/presentation/
