# One file, several mapping interfaces, and two keys that belong to neither of them.
app.name = acme
app.timeout = 30

db.host = localhost
db.port = 5432

myproject.prefix = myproject
myproject.debug = true

server.alpha.host = alpha.example.com

logging.level = INFO
spring.datasource.url = jdbc:h2:mem:test

# named exactly like methods of Accessible, Mutable and Reloadable, which is what makes
# theMethodsOfTheLibraryItselfAreNotDeclaredKeys worth writing
store = somebody else's key
clear = and another
reload = and a third
