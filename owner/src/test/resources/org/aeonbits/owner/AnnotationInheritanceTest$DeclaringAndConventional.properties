# The file the convention would find for AnnotationInheritanceTest.DeclaringAndConventional, which declares
# its own @Sources. Since 2.0.0 the convention is a fallback for a configuration that names no source, not an
# extra source appended to the ones it named, so nothing here is ever read - which is what the test asserts.
foo=convention
bar=convention
